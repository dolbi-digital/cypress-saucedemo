# 🧪 Cypress E2E Tests

| Command | Description |
|----------|-------------|
| `npx cypress run --browser firefox` | Run tests locally in a specific browser |
| `npm run cy:run:all` | Run tests sequentially in all browsers (Chrome, Firefox, Edge) |